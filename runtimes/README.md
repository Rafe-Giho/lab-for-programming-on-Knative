# runtimes

이 디렉터리에는 언어별 실행 이미지를 둡니다.

이 이미지들은 직접 외부에 노출되지 않습니다.

- 포털이 언어와 버전을 선택합니다.
- 포털이 `code-runner-exec` 네임스페이스에 Job을 생성합니다.
- Job Pod가 여기 있는 이미지 중 하나를 사용합니다.

현재 운영 기준은 아래와 같습니다.

- `python/`: `3.11`
- `java/`: `17`
- `c/`: `gcc-14`
- `cpp/`: `g++-14`
